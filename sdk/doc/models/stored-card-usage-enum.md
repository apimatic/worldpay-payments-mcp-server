
# Stored Card Usage Enum

Set to `first` to store a card or `subsequent` to use a previously stored card.

## Enumeration

`StoredCardUsageEnum`

## Fields

| Name |
|  --- |
| `First` |
| `Subsequent` |

